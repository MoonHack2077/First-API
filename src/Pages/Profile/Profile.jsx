import React from 'react';

function Profile( { character } ){
    return(
        <ul>
            <li>{character.name}</li>
            <li>{character.name}</li>
            <li>{character.name}</li>
            <li>{character.name}</li>
            <li>{character.name}</li>
        </ul>
    )
}

export { Profile }